# 測定指標一覧（修了制作 / 検証ハーネス）

添付プレゼン「修了制作.pptx」の **P13〜P16** で設計された「本プロジェクトで測定する指標」を表にまとめ、
併せて **現状ハーネスで実装済みの計測** を調査し、各指標が **どこからどこまでを測っているのか（ユーザー目線）**
を対応づけたもの。

- 出典スライド: P13=評価の骨格 / P14=①効率カード / P15=②品質カード / P16=③自動化率カード
  （ユーザー指示は「P13〜15」だが、指標カードは③自動化率がP16に続くため、設計の完全性を保つためP16も含めた）
- ★ = 発表で用いる**主指標**（P13の骨格図で指定。青＝押し上げる力／緑＝守る対象）

## 検証の骨格（P13）

> **自動化率↑・効率↑ でも 品質は"維持"されるか** を検証する。

| 立場 | 意味 | 中心となる主指標 |
|------|------|------------------|
| 押し上げる力（青） | 自動化率↑・効率↑ | 人時比率 / 自動化率、コードレビュー省略率、人手レビュー時間 |
| 守る対象（緑） | 品質は維持が目標（既存と同等以上） | 受入条件充足率、標準偏差 / 変動係数 |
| 核心の問い | レビュー省略率↑で欠陥・デグレは増えないか | 「**コードレビュー省略率 × 残存欠陥・デグレ**」のペアが提案プロセスの成否を直接示す |

---

## 実装状況の凡例

| 記号 | 意味 |
|------|------|
| ✅ | **自動計測 実装済み**（`.harness/metrics.jsonl` 等に自動記録される） |
| 🟡 | **部分実装 / 導出可能**（生データは取得済みだが、指標としての集計・算出は未実装） |
| ⚙️ | **手動運用**（仕組み上は人が記録する前提。自動計測なし） |
| 🔴 | **未実装**（計測手段・対象プロセスがまだ無い） |

> ハーネスが現在自動で持つ生データは、各フェーズ実行ごとに `.harness/metrics.jsonl` へ追記される
> 1行1JSON（`ts / story / variant / phase / is_error / duration_ms / num_turns / cost_usd / session_id`、
> 巻き戻し時は `rolled_back=true`）のみ。集計・統計・網羅度・欠陥検出などの上位指標を出す
> スクリプトは現時点で存在しない（`Invoke-Process.ps1` が生ログを吐くところまで）。

---

## ① 効率（P14｜観点でグルーピング）

| 観点 | 指標名 | 定義（PPT） | 出典 | 実装 | 測定範囲（どこから→どこまで／ユーザー目線） |
|------|--------|-------------|------|:---:|-----------------------------------------------|
| 時間 | ★ 人手レビュー時間 | 承認までの時間・目標 −30%以上 | Kemerer & Paulk | ⚙️ | 各ゲートで人がPRを開いてレビューし承認/差し戻すまでの**実時間**。**自動計測なし**（READMEでも「人が記録」と明記）。開始=ゲート停止でPR提示、終了=`-Continue`/`-Revise`判断。ハーネスは時刻を打っていない。 |
| 時間 | リードタイム / フェーズ別実行時間 | US入力→全テスト合格の総時間・工程別 duration | MetaGPT・ChatDev・arXiv:2509.13942 | 🟡 | **フェーズ別**は✅記録済み（`duration_ms`＝`claude -p`呼び出し開始〜`result`返却まで。AI処理の実時間のみで、ゲート停止中や人のレビュー時間は**含まない**）。**総リードタイム**（US入力→全テスト合格）は全フェーズの合算で導出可能だが集計スクリプト未実装。 |
| コスト | ★ 人的工数 | レビュー時間＋差し戻し対応時間 | arXiv:2411.12924 | ⚙️/🟡 | 人のレビュー時間は⚙️未計測。差し戻し対応のうち**AI再実行時間**は`phase="<id>-revise"`の`duration_ms`で🟡取得済みだが、これはAIの実行時間であり「人の差し戻し対応時間」とは別物。人手側の工数は未計測。 |
| コスト | 生成AI利用料 | トークン量×単価・US1件で比較 | SWE-bench | 🟡 | **金額**は✅記録済み（`cost_usd`＝`total_cost_usd`、フェーズ単位・US1件）。ただし「**トークン量×単価**」の内訳（入出力トークン数）は取得しておらず、金額のみ。 |
| レビュー効率 | 指摘密度 | レビュー指摘数 ÷ レビュー時間 | Jones | 🔴 | レビュー指摘（PRコメント）は`-Revise`時にgh経由で**収集する仕組みはある**が、件数を指標として記録していない。分母のレビュー時間も未計測。指標としては未算出。 |
| レビュー効率 | レビュー対象数 | 成果物件数・コード行数（PR diff） | — | 🔴 | PR diff の行数・ファイル数は git/gh から取得可能だが、記録・集計は未実装。 |
| 手戻り | 差し戻し回数・手戻り時間 | ゲート不合格→再実行の回数と所要時間（state.json） | — | 🟡 | **回数**＝`metrics.jsonl`の`phase`末尾が`-revise`の行数で導出可（記録済み）。**時間**＝その`-revise`行の`duration_ms`（AI再実行時間）で取得済み。ただし件数集計・「ゲート不合格判定」自体は人手依存で、自動サマリは未実装。 |

> P14補足: 時間・コストは n=10 の平均±標準偏差で報告予定（→ ③のばらつき指標と連動）。

---

## ② 品質（P15｜維持が目標）

| 観点 | 指標名 | 定義（PPT） | 出典 | 実装 | 測定範囲（どこから→どこまで／ユーザー目線） |
|------|--------|-------------|------|:---:|-----------------------------------------------|
| 機能的正しさ | ★ 受入条件充足率 | 合格AC ÷ 全AC・E2Eで判定 | SWE-bench | 🔴 | 分母の受入条件は `stories/US-001/acceptance-criteria.md` に**枠はあるが未記入（AC-1〜3 が「（未記入）」）**。E2Eでの合否判定・充足率の自動算出は未実装。 |
| テスト自身の質 | Mutation Score | テストが欠陥を検出できるか | Papadakis+ ICSE2018 | 🔴 | ミューテーションテスト実行基盤・計測ともに未実装。 |
| 欠陥検出 | 工程内封じ込め率(PCE) | 上流検出の割合・PRに検出工程を付与 | DRE / Capers Jones | ⚙️/🔴 | `stories/US-001/injected-defects.md` に「実際に検出された工程」欄はあるが**未記入・手動運用前提**。PRへ検出工程を付与する自動化・PCE算出は未実装。※P15で「仕込み欠陥検出率は**不採用**」とある一方、注入欠陥ファイルは残存＝設計と実装に**不整合**（要確認）。 |
| デグレ | 回帰テスト合格率 | 既存テストの合格率・回帰修正回数 | SWE-bench pass-to-pass | 🔴 | 回帰テストスイートの合否収集・修正回数カウントは未実装。 |
| 非機能・保守性 | 保守性に関する指摘件数 | Lint違反・code smell 等の件数で代替 | arXiv:2509.13942 | 🔴 | Lint / 静的解析の実行・件数集計は未実装。 |
| ばらつき | ★ 標準偏差・変動係数 | n=10 反復の分布・目標：既存より小 | LLMの非決定性 | 🟡 | n=10反復の生データ（`duration_ms`/`cost_usd`）は✅蓄積される設計（worktree隔離＋`metrics.jsonl`追記、`rolled_back`除外で最終試行のみ集計する運用）。ただし**標準偏差・変動係数の算出スクリプトは未実装**。 |

> P15補足: 品質は「維持」（既存と同等以上）が目標。**トレーサビリティ網羅度・カバレッジ・仕込み欠陥検出率は不採用**。

---

## ③ 自動化率（P16｜自動化↑・効率↑で品質維持できるか）

| 観点 | 指標名 | 定義（PPT） | 出典 | 実装 | 測定範囲（どこから→どこまで／ユーザー目線） |
|------|--------|-------------|------|:---:|-----------------------------------------------|
| 時間ベース | ★ 人時比率 | 人的工数 ÷ 総リードタイム。自動化率＝1−人時比率（人的工数は①効率の値を用いる） | approval-gate latency | 🔴 | 分子の**人的工数（①）が未計測**のため算出不可。分母の総リードタイムは`duration_ms`合算で導出可能。人時比率・自動化率ともに未算出。 |
| レビュー省略 | ★ コードレビュー省略率 | テスト合格で省略できた対象 ÷ 全対象。提案プロセスの核 | arXiv:2402.13521 | 🔴 | 「テスト合格でレビューを省略する」**提案プロセス（proposed）自体が未実装**（現状は existing / baseline のみ）。省略対象のカウント手段が無く測定不可。 |
| 自律完遂 | ゲート一発通過率 | 差し戻しゼロで承認された割合（state.json） | HITL_Necessity_Rate | 🟡 | 各フェーズで`-revise`が0のまま`-Continue`された割合＝一発通過。生データ（`-revise`行の有無）から導出可能だが、判定・集計は未実装。 |

> P16補足: 「**コードレビュー省略率 × 残存欠陥・デグレ**」のペアが提案プロセスの成否を直接示す（＝核心の問い）。

---

## 現状ハーネスで「自動的に測れているもの」まとめ（ユーザー目線の測定範囲）

`Invoke-Process.ps1` が `claude -p --output-format stream-json` の最終 `result` イベントから拾い、
`.harness/metrics.jsonl` へ**フェーズ実行ごとに1行**追記する。

| フィールド | 何を測っているか | 測定の開始点 → 終了点 | 対応する設計指標 |
|-----------|------------------|------------------------|-------------------|
| `duration_ms` | そのフェーズのAI実行の実時間 | プロンプトを`claude -p`へ投入 → `result`返却 | リードタイム/フェーズ別実行時間（①）、手戻り時間（①） |
| `cost_usd` | そのフェーズのClaude API利用料（USD） | 同フェーズの1回の`claude -p`呼び出し全体 | 生成AI利用料（①）※金額のみ |
| `num_turns` | アシスタントのターン数（補助情報） | 同フェーズ内 | （PPT指標に直接対応なし・参考値） |
| `phase`（`-revise`） | 差し戻し再実行の回数・時間の識別 | `-Revise`実行ごとに`<id>-revise`行を追加 | 差し戻し回数・手戻り時間（①）、ゲート一発通過率（③） |
| `rolled_back` | 巻き戻し済み試行の除外フラグ | `-Rollback`時に該当行へ付与 | 標準偏差・変動係数の集計時に最終試行のみ数えるため（②） |
| `is_error` / `session_id` / `ts` / `story` / `variant` | 実行の成否・追跡・条件識別 | 各フェーズ実行時 | 集計時の分類キー |

**測定範囲の重要な境界（誤解しやすい点）:**

- `duration_ms` は **AIが動いている時間のみ**。ゲートで停止して人がレビューしている時間、
  `-Continue` を叩くまでの待ち時間は**一切含まない**。
- したがって現状の自動計測だけでは「**人手レビュー時間**」「**人的工数**」は取れず、
  それに依存する「**人時比率・自動化率**」も算出できない（人手側の計測は⚙️手動運用として別途必要）。
- 「総リードタイム」「手戻り回数」「ゲート一発通過率」「標準偏差」は
  **生データはあるが集計スクリプトが未実装**（🟡）。集計器を1本足せば発表用の値になる状態。
- 品質系（受入条件充足率・Mutation・PCE・回帰・保守性）と③のレビュー省略率は
  **計測基盤そのものが未整備**（🔴）。特にコードレビュー省略率は**提案プロセス未実装**が前提ブロッカー。

## 実装の優先度メモ（一指標ずつ着手する際の目安）

1. **集計器を追加すれば取れる（🟡→✅）**: 総リードタイム、生成AI利用料合計、手戻り回数、
   ゲート一発通過率、標準偏差・変動係数 … `metrics.jsonl`を読む集計スクリプト1本で主指標の一部が揃う。
2. **人手計測の仕組み化（⚙️）**: 人手レビュー時間・人的工数 … ゲートの開始/承認時刻を`state.json`に
   打刻するなどの軽い計装で自動化余地あり。①・③の主指標のブロッカー。
3. **品質計測基盤の新規実装（🔴）**: 受入条件充足率（AC記入＋E2E判定連携）→ 回帰テスト合格率 →
   保守性指摘件数（Lint）→ Mutation Score → PCE の順で段階導入。
4. **提案プロセス実装（🔴・前提）**: `processes/proposed.json`＋`prompts/proposed/*.md`が無いと
   コードレビュー省略率（③の核）は測れない。

> 注意: `injected-defects.md`（欠陥注入＝仕込み欠陥検出率の枠組み）は残っているが、
> P15で「仕込み欠陥検出率は不採用」と決定されている。設計と実装資産の不整合として、
> 採否を確定させておくとよい（PCE/DREに一本化するか、注入欠陥を廃止するか）。
